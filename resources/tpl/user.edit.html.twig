{% extends 'base.default.html.twig' %}

{% block content %}


<h1><i class="icon-plus-sign"></i> Benutzer editieren</h1>


<div class="row">
    <div class="span12">          
        <form method="post" class="form-horizontal">
            <div class="control-group">
               {{ form_label(form.userName, "", { 'label_attr': { 'class' : 'control-label' } }) }} 
               <div class="controls">
               {{ form_widget(form.userName) }}
               </div>
            </div>

            <div class="control-group">
               {{ form_label(form.area, "", { 'label_attr': { 'class' : 'control-label' } }) }} 
               <div class="controls">
               {{ form_widget(form.area) }}
               </div>
            </div>

            <div class="control-group">
               {{ form_label(form.privileges, "", { 'label_attr': { 'class' : 'control-label' } }) }} 
               <div class="controls">
               {{ form_widget(form.privileges) }}
               </div>
            </div>
            {{ form_rest(form) }} 
            <div class="control-group">    
                <div class="controls">
                    <button type="submit" name="submit" class="btn btn-primary">Benutzer updaten</button>
                </div>                
            </div>
        </form>
    </div>
</div>
{% endblock %}
